{
  "name": "ecoshift-custom-code",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "esbuild app.js --bundle --outfile=dist/app.js --watch"

  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.21.5",
    "serve": "^14.2.3"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  }
}
